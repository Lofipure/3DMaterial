// ? 用户性别
export enum SexType {
  male = 1,
  female = 2,
}
// ? 模型权限
export enum AuthControl {
  // private = 1,
  public = 2,
  protected = 3,
}
// ? 用户触发事件类型
export enum EventType {
  create = 1,
  modify = 2,
  visit = 3,
  praise = 4,
}
// ? 实体类型
export enum EntityType {
  User = 1,
  Tag = 2,
  Model = 3,
}
